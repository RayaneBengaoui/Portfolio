//React Router
import { Link } from "react-router-dom";
//Styled Components
import styled from "styled-components";

const Nav = () => {
  return (
    <div>
      <h2>Hello La nav</h2>
    </div>
  );
};

export default Nav;
